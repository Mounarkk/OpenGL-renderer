# Renderer configuration, read at startup.
# Format: key = value. Relative paths are resolved from the project root.

window.width = 1280
window.height = 720
renderer.vsync = true

paths.shaders = res/shaders/
paths.models = res/models/
paths.textures = res/textures/
paths.skybox = res/skyboxes/arctic/
paths.screenshots = screenshots/

camera.fov = 45.0
camera.speed = 5.0
camera.sensitivity = 0.1
camera.near_plane = 0.1
camera.far_plane = 150.0

# Resolution of each cascade of the directional shadow map.
shadow.map_size = 2048
